% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlacion_info.r
\name{calcular_correlacion}
\alias{calcular_correlacion}
\title{Cálculo de correlación/asociación entre atributos.}
\usage{
calcular_correlacion(
  dataset,
  atributos = NULL,
  normalizar = FALSE,
  verbose = 1
)
}
\arguments{
\item{dataset}{DataFrame sobre el que realizar el cálculo.}

\item{atributos}{Lista de nombres de columnas a analizar, o NULL para usar todas las columnas del dataset.}

\item{normalizar}{Si es TRUE, se normalizan los resultados a una escala 0-1 (actualmente no implementado).}

\item{verbose}{Nivel de información que se muestra durante la ejecución.}
}
\value{
Un list con dos elementos: \code{matriz} (DataFrame numérico de correlaciones/asociaciones entre los atributos) y \code{detalles} (diccionario con toda la información adicional de cada par de atributos, indexado por la tupla (atributo1, atributo2)).
}
\description{
Calcula una matriz de correlación/asociación entre los atributos
seleccionados del dataset.
}
\details{
Casos contemplados:

\itemize{
\item numérica + numérica -> Correlación de Pearson
\item categórica + categórica -> Información Mutua (Mutual Information)
\item categórica + numérica -> ANOVA (Welch)
}
}
