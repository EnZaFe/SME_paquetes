% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metricas_de_variables.r
\name{.calcular_AUC}
\alias{.calcular_AUC}
\title{Calcula el AUC (Área bajo la curva ROC) por comparación de pares entre las dos clases.
Cuenta cuántas veces un valor de la clase 1 supera a uno de la clase 2, promediando los empates como 0.5. Al ser binario,
\code{auc} y \code{1 - auc} describen el mismo separador, por lo que se devuelve \code{max(auc, 1 - auc)}.}
\usage{
.calcular_AUC(atributo_clm, clases, verbose)
}
\arguments{
\item{atributo_clm}{Vector de valores continuos de la variable objetivo.}

\item{clases}{Vector binario con las etiquetas de clase (debe tener el mismo tamaño que \code{atributo_clm} y exactamente dos clases).}

\item{verbose}{Nivel de verbosidad.}
}
\value{
El valor del AUC calculado.
}
\description{
Calcula el AUC (Área bajo la curva ROC) por comparación de pares entre las dos clases.
Cuenta cuántas veces un valor de la clase 1 supera a uno de la clase 2, promediando los empates como 0.5. Al ser binario,
\code{auc} y \code{1 - auc} describen el mismo separador, por lo que se devuelve \code{max(auc, 1 - auc)}.
}
