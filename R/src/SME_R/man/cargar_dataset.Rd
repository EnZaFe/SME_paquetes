% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_datasets.R
\name{cargar_dataset}
\alias{cargar_dataset}
\title{Cargar un dataset}
\usage{
cargar_dataset(
  dataset = "iris",
  sep = NULL,
  encoding = NULL,
  hoja = 0,
  verbose = 1,
  ...
)
}
\arguments{
\item{dataset}{\code{character} o \code{data.frame}. Dataset que se desea cargar.
Puede ser:
\itemize{
\item Un nombre de dataset, como \code{"iris"}, \code{"wine"} o
\code{"breast_cancer"}.
\item Una ruta a un archivo \code{.csv}, \code{.txt}, \code{.tsv}, \code{.tab},
\code{.xlsx}, \code{.xlsm}, \code{.xls} u \code{.ods}.
\item Un \code{data.frame}, que se devuelve tal cual.
}}

\item{sep}{\code{character} opcional. Separador utilizado para archivos de
texto. Si es \code{NULL}, se intenta autodetectar entre \verb{,}, \verb{;}, tabulador
y \code{|}. Para archivos \code{.tsv} y \code{.tab} se utiliza tabulador por defecto.}

\item{encoding}{\code{character} opcional. Codificación del archivo. Si es
\code{NULL}, se prueban codificaciones habituales como \code{"UTF-8"} y
\code{"latin1"}.}

\item{hoja}{\code{integer} o \code{character}. Hoja que se desea cargar en archivos
Excel. Puede ser un índice (\code{0} para la primera hoja) o el nombre de
la hoja.}

\item{verbose}{\code{integer}. Nivel de mensajes informativos. \code{0} no muestra
mensajes y \code{1} muestra mensajes básicos durante la carga.}

\item{...}{Argumentos adicionales que se pasan al lector correspondiente,
como \code{readr::read_csv()}, \code{readr::read_tsv()} o
\code{readxl::read_excel()}.}
}
\value{
Un \code{data.frame} que contiene el dataset cargado.
}
\description{
Carga un dataset a partir de su nombre, una ruta de archivo o un
\code{data.frame} y devuelve un \code{data.frame}.
}
\details{
Permite cargar datasets de ejemplo por nombre, archivos CSV/TSV y
archivos Excel. También acepta directamente un \code{data.frame}, que se
devuelve sin modificaciones.
}
\examples{
# Cargar el dataset iris
df <- cargar_dataset("iris")

# Cargar directamente un data.frame
datos <- data.frame(
  x = 1:3,
  y = c("a", "b", "c")
)
df <- cargar_dataset(datos)

}
