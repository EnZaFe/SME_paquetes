% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metricas_de_variables.r
\name{calcular_metricas}
\alias{calcular_metricas}
\title{Cálculo de métricas para los atributos de un dataset: entropía para las discretas, varianza y AUC para las continuas.
La función reconoce el tipo de atributo y actúa en consecuencia. En el caso del AUC, el dataset debe ser supervisado,
es decir, es necesario especificar una variable clase binaria con la que evaluar el AUC de los atributos numéricos.}
\usage{
calcular_metricas(dataset, clases = NULL, atributos = NULL, verbose = 1)
}
\arguments{
\item{dataset}{Datos de entrada. Puede ser un vector (variable) o un data.frame.}

\item{clases}{Columna binaria opcional con la que evaluar el AUC de las variables continuas.}

\item{atributos}{Columnas específicas a analizar. Si es NULL, se analizan todas las columnas.}

\item{verbose}{Nivel de verbosidad.}
}
\value{
Para una variable discreta: lista con \code{entropia}. Para una continua sin clases: lista con \code{varianza}. Para una continua con clases: lista con \code{varianza} y \code{auc}. Para un dataset, el mismo dataset con las columnas anotadas con sus métricas.
}
\description{
Cálculo de métricas para los atributos de un dataset: entropía para las discretas, varianza y AUC para las continuas.
La función reconoce el tipo de atributo y actúa en consecuencia. En el caso del AUC, el dataset debe ser supervisado,
es decir, es necesario especificar una variable clase binaria con la que evaluar el AUC de los atributos numéricos.
}
