% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualizacion.r
\name{graficar_auc}
\alias{graficar_auc}
\title{Gráfico de la curva ROC y la distribución del atributo por clase.}
\usage{
graficar_auc(
  atributo_clm,
  clases,
  resultado = NULL,
  pregunta = NULL,
  verbose = 1,
  path = ".",
  nombre_carpeta = "figures"
)
}
\arguments{
\item{atributo_clm}{Serie numérica (score/probabilidad del modelo).}

\item{clases}{Vector de etiquetas de clase, exactamente dos. Deben tener el
mismo número de elementos que \code{atributo_clm}.}

\item{resultado}{Lista opcional con el AUC ya calculado. Si se pasa, no se recalcula.}

\item{pregunta}{Pregunta que responde el gráfico. Si es \code{NULL}, se genera una automáticamente.}

\item{verbose}{Nivel de información mostrado durante la ejecución.}

\item{path}{Carpeta base donde se guarda la imagen.}

\item{nombre_carpeta}{Nombre de la subcarpeta (por defecto \code{"figures"}).}
}
\value{
Un objeto \code{ggplot} con la curva ROC y la distribución por clase, guardado como \code{demo_auc.png}.
}
\description{
Curva ROC + distribución del atributo numérico por clase. Se puede llamar
de dos formas: con el AUC calculado automáticamente o reutilizando un AUC ya
calculado (evitando recalcularlo).
}
\section{Uso}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{graficar_auc(score, clases) -> fig con la curva ROC y la densidad por clase
graficar_auc(score, clases, resultado = info) -> reutiliza un AUC ya calculado
}\if{html}{\out{</div>}}
}

