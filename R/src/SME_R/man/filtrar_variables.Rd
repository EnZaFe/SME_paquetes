% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filtrado_variables.r
\name{filtrar_variables}
\alias{filtrar_variables}
\title{Filtra las variables de un dataset según sus métricas.}
\usage{
filtrar_variables(
  dataset,
  entropia = NULL,
  varianza = NULL,
  auc = NULL,
  modo = "dentro",
  clases = NULL,
  atributos = NULL,
  metricas = NULL,
  verbose = 1
)
}
\arguments{
\item{dataset}{DataFrame que queremos filtrar.}

\item{entropia}{\verb{(min, max)} para filtrar variables discretas. Los extremos pueden ser \code{NULL} para dejarlos abiertos.}

\item{varianza}{\verb{(min, max)} para filtrar variables continuas. Los extremos pueden ser \code{NULL} para dejarlos abiertos.}

\item{auc}{\verb{(min, max)} para filtrar variables continuas. Los extremos pueden ser \code{NULL} para dejarlos abiertos.}

\item{modo}{\code{"dentro"} (el valor está dentro del intervalo) o \code{"fuera"} (el valor está fuera del intervalo).}

\item{clases}{Vector binario con las etiquetas de clase, necesario para calcular el AUC.}

\item{atributos}{Lista opcional de variables a evaluar. Si es \code{NULL}, todas.}

\item{metricas}{Diccionario de métricas precalculadas \code{{columna: {...}}}. Si es \code{NULL}, se calculan automáticamente.}

\item{verbose}{Nivel de información que se muestra durante la ejecución.}
}
\value{
Un data.frame con las variables que cumplen los requisitos especificados.
}
\description{
Se pueden combinar varios criterios (entropía, varianza y/o AUC). Una
variable se conserva solo si cumple todas las condiciones especificadas.
Si \code{metricas} no se pasa, se calculan automáticamente.
}
\section{Uso}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{filtrar_variables(df, entropia = (0.5, 1)) -> df con variables que cumplen
filtrar_variables(df, varianza = (5, 7), modo = "dentro") -> df filtrado
}\if{html}{\out{</div>}}
}

